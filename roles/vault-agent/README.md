# Vault Agent

This role installs Vault and configures it to run Vault in
[Agent mode](https://www.vaultproject.io/docs/agent/index.html).

It will then authenticate with a Vault server using the EC2 metadata.
